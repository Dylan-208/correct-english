<Application x:Class="CorrectEnglish.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>

    <!-- Paleta do mockup em docs/plano.html. Tema claro apenas na Fase 1;
         seguir o tema do Windows entra na Fase 4. -->
    <SolidColorBrush x:Key="Surface"     Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="SurfaceAlt"  Color="#FFEDEFF3" />
    <SolidColorBrush x:Key="Sunken"      Color="#FFE4E7ED" />
    <SolidColorBrush x:Key="Ink"         Color="#FF151A22" />
    <SolidColorBrush x:Key="InkSoft"     Color="#FF3D4654" />
    <SolidColorBrush x:Key="Muted"       Color="#FF6B7686" />
    <SolidColorBrush x:Key="Line"        Color="#FFDCE0E7" />
    <SolidColorBrush x:Key="Accent"      Color="#FFC0342F" />
    <SolidColorBrush x:Key="AccentSoft"  Color="#FFF7E4E3" />
    <SolidColorBrush x:Key="Ok"          Color="#FF1C7A4B" />
    <SolidColorBrush x:Key="OkSoft"      Color="#FFE3F0E9" />

    <FontFamily x:Key="UiFont">Segoe UI Variable Text, Segoe UI</FontFamily>
    <FontFamily x:Key="MonoFont">Consolas, Cascadia Mono</FontFamily>

    <!-- Rotulo de secao: SELECIONADO, PORTUGUES, CORRECAO, POR QUE -->
    <Style x:Key="SectionLabel" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
      <Setter Property="FontSize" Value="9.5" />
      <Setter Property="Foreground" Value="{StaticResource Muted}" />
      <Setter Property="Margin" Value="0,0,0,5" />
    </Style>

    <Style x:Key="BodyText" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Foreground" Value="{StaticResource Ink}" />
      <Setter Property="TextWrapping" Value="Wrap" />
      <Setter Property="LineHeight" Value="21" />
    </Style>

    <Style x:Key="QuotedText" TargetType="TextBlock" BasedOn="{StaticResource BodyText}">
      <Setter Property="FontSize" Value="13.5" />
      <Setter Property="Foreground" Value="{StaticResource InkSoft}" />
    </Style>

    <!-- Botao base, cantos arredondados -->
    <Style x:Key="FlatButton" TargetType="Button">
      <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource Ink}" />
      <Setter Property="Background" Value="{StaticResource Surface}" />
      <Setter Property="BorderBrush" Value="{StaticResource Line}" />
      <Setter Property="Padding" Value="13,6" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="SnapsToDevicePixels" Value="True" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.85" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Ink}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
      <Setter Property="Background" Value="{StaticResource Accent}" />
      <Setter Property="BorderBrush" Value="{StaticResource Accent}" />
      <Setter Property="Foreground" Value="White" />
    </Style>

  </Application.Resources>
</Application>
